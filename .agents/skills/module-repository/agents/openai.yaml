interface:
  display_name: "Module Repository"
  short_description: "Criar contrato de repositorio e fake em memoria"
  default_prompt: "Use $module-repository para criar um repositorio de agregado em modules/<modulo>, reaproveitando contratos do namespace compartilhado do projeto (derivado do package.json) e gerando tambem uma fake in-memory para testes."
