/**
 * TEMPLATE — page.tsx de módulo privado
 *
 * Crie um arquivo como este para cada página de módulo em (private)/{module}/page.tsx.
 * Substitua 'ModuleName' e 'Module Label' pelo nome real.
 */

export default function ModuleNamePage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Module Label</h1>
      <p className="text-muted-foreground mt-1">Em desenvolvimento.</p>
    </div>
  );
}
