interface:
  display_name: "Config Prisma"
  short_description: "Inicializar Prisma e banco no backend"
  default_prompt: "Inicialize e padronize a infraestrutura Prisma no backend NestJS com seed técnico em prisma/seed/main.ts (sem seeds de módulos), docker-compose compatível com DATABASE_URL do .env e com nomes derivados do projeto, schema modular por módulo e DbModule + PrismaService simples no padrão atual."
